import { StyleSheet } from 'react-native';
import { spacing } from '@rn-ds/tokens';

export const styles = StyleSheet.create({
  clearHit: {
    paddingHorizontal: spacing.s1,
    paddingVertical: spacing.s1,
  },
  meta: {
    marginTop: spacing.s1,
    paddingHorizontal: spacing.s1,
  },
});
